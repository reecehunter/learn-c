// Tell the program to include functions from the standard I/O library.
#include <stdio.h>

// Define the main entry point for the program.
// Should return 0 if successful and -1 if not.
int main() {
	// Log "Hello world!" to the command line.
	// The "\n" is added to move the cursor to the next line as good practice.
	printf("Hello world!\n");

	// Return 0 to indicate a successful run.
	return 0;
}
